# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "development" branch
  push:
    branches: [ "development" ]
  pull_request:
    branches: [ "development" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      - uses: actions/setup-node@v1
        with:
          node-version: 12
      - run: npm install -g yarn
      - run: yarn install --frozen-lockfile
      - run: yarn build
      - run: ls -la
      - run: ls -la .next/
      - run: ls -la src/
      - run: ls -la public/
#      - name: Sync files to hosting 📁📁
#        uses: SamKirkland/FTP-Deploy-Action@4.1.0
#        with:
#          server: ftp.designhouse.dev
#          username: u270232017.dh_github_ci_cd
#          password: ${{ secrets.ftp_pass }}
#          local-dir: src/out/
      
      
      total 192
drwxr-xr-x   8 runner docker   4096 Jun 28 00:37 .
drwxr-xr-x   3 runner docker   4096 Jun 28 00:36 ..
-rw-r--r--   1 runner docker    155 Jun 28 00:37 .babelrc
-rw-r--r--   1 runner docker     40 Jun 28 00:37 .eslintrc.json
drwxr-xr-x   8 runner docker   4096 Jun 28 00:37 .git
drwxr-xr-x   3 runner docker   4096 Jun 28 00:37 .github
-rw-r--r--   1 runner docker    343 Jun 28 00:37 .gitignore
drwxr-xr-x   5 runner docker   4096 Jun 28 00:37 .next
-rw-r--r--   1 runner docker   1581 Jun 28 00:37 README.md
drwxr-xr-x 258 runner docker  12288 Jun 28 00:37 node_modules
-rw-r--r--   1 runner docker    526 Jun 28 00:37 package.json
drwxr-xr-x   2 runner docker   4096 Jun 28 00:37 public
drwxr-xr-x   6 runner docker   4096 Jun 28 00:37 src
-rw-r--r--   1 runner docker 131971 Jun 28 00:37 yarn.lock
